{
  "name": "bongsh",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/client"
  ],
  "scripts": {
    "build:client": "npm run build --workspace=client && cp -r apps/client/dist/* apps/server/public",
    "build": "npm run build:client"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "volta": {
    "node": "24.13.1"
  }
}
